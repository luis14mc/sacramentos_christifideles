# ChristiFidelis - variables por entorno
# Mantener archivos .env fuera del control de versiones.

# PostgreSQL local.
DATABASE_URL="postgresql://usuario:password@localhost:5432/christifideles_local"
DIRECT_URL="postgresql://usuario:password@localhost:5432/christifideles_local"

# Base aislada y desechable para `pnpm test`. Es obligatoria: las suites no usan
# DATABASE_URL directamente. Usar localhost o un objetivo marcado test/testing.
TEST_DATABASE_URL="postgresql://usuario:password@localhost:5432/christifideles_test"

# Neon: DATABASE_URL usa el endpoint pooled (-pooler). DIRECT_URL usa el endpoint
# directo (sin -pooler) para migraciones. SSL obligatorio en ambos.
# DATABASE_URL="postgresql://usuario:password@ep-xxxx-pooler.region.aws.neon.tech/ChristiFideles?sslmode=require"
# DIRECT_URL="postgresql://usuario:password@ep-xxxx.region.aws.neon.tech/ChristiFideles?sslmode=require"

# NextAuth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="reemplazar-por-secreto-largo-y-aleatorio"

# Setup inicial: mantener false salvo bootstrap controlado de una base vacía.
ALLOW_INITIAL_SETUP="false"

# Seed administrativo exclusivo de development/testing; se bloquea en production.
# Con NODE_ENV=development/test, si se omiten, se usan las credenciales conocidas
# documentadas para testing. En cualquier otro ambiente ambas son obligatorias.
SEED_SUPERADMIN_EMAIL=
SEED_SUPERADMIN_PASSWORD=

# Identidad de la instancia (una instancia + BD por parroquia).
# Ver docs/PLAN_MULTIPARROQUIA.md. Sin PARROQUIA_NOMBRE el seed usa Cristo Resucitado.
PARROQUIA_CODIGO=cristo-resucitado
PARROQUIA_NOMBRE=
PARROQUIA_UBICACION=0801
PARROQUIA_DIRECCION=
PARROQUIA_TELEFONO=
PARROQUIA_EMAIL=

# Doble confirmación requerida por `pnpm db:reset:dev`. Nunca habilitar en producción.
ALLOW_DB_RESET="false"

# Variables opcionales para seed/smoke de QA. No hardcodear credenciales reales.
QA_ADMIN_EMAIL="qa-admin@example.test"
QA_ADMIN_PASSWORD="reemplazar-en-entorno-qa"
BASE_URL="http://localhost:3000"

# DEMO (staging / Railway / local). Requeridas solo para `pnpm db:seed:demo`.
# Las contraseñas tienen >= 8 caracteres y NO se imprimen en logs.
# Los emails resultantes son demo-admin@cristoresucitado.org,
# demo-secretario@cristoresucitado.org y demo-catequista@cristoresucitado.org.
DEMO_ADMIN_PASSWORD=
DEMO_SECRETARIO_PASSWORD=
DEMO_CATEQUISTA_PASSWORD=

# Guard obligatorio para `pnpm db:seed:demo`. Aunque NODE_ENV sea
# 'production' (caso Railway), este seed ES DEMO: nunca debe ejecutarse
# por accidente. Solo corre con ALLOW_DEMO_SEED=true explícito.
# Uso tipico (BD Railway):
#   export ALLOW_DEMO_SEED=true
#   pnpm db:seed:demo
#   # al terminar:
#   unset ALLOW_DEMO_SEED
ALLOW_DEMO_SEED=false

# RAILWAY (Web + PostgreSQL plugin)
# DATABASE_URL="${{Postgres.DATABASE_URL}}"   # referencia en dashboard Railway
# DIRECT_URL="${{Postgres.DATABASE_URL}}"     # misma URL en Railway (sin pooler separado)
# NEXTAUTH_URL="https://tu-servicio.up.railway.app"
# NEXTAUTH_SECRET="generar-un-secreto-largo-y-distinto"
# ALLOW_INITIAL_SETUP="false"
# PORT lo define Railway; no hace falta fijarlo a mano.

# VERCEL + NEON (alternativa)
# DATABASE_URL="postgresql://...@...-pooler....neon.tech/db?sslmode=require"
# DIRECT_URL="postgresql://...@....neon.tech/db?sslmode=require"
# NEXTAUTH_URL="https://tu-deployment.vercel.app"
#
# Migraciones: usar `prisma migrate deploy` para aplicar migraciones versionadas
# (prisma/migrations/) en CI, staging y producción. Reservar `prisma db push`
# solo para prototipado local puntual, nunca para entornos compartidos.
